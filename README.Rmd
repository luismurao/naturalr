## Install

Install the development version using `install_github` within Hadley's [devtools](https://github.com/hadley/devtools) package.

```{r, eval=FALSE}
install.packages("devtools")
require(devtools)

install_github("naturalr", "luismurao")
library(naturalr)

```
